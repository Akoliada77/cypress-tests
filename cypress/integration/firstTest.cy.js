/// <reference types="cypress" />

import './support/commands.js'

describe('example to-do app', () => {
    beforeEach(() => {
    })

    it('Login as a colloborator', () => {
      cy.visit('https://rubyroidlabs.dev/contact')
   })
})